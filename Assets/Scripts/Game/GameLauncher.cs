using System;
using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class GameLauncher : MonoBehaviour
{
/*    public void LaunchTheGame()
   {
        Action createGemTaskCallback = delegate()
        {

        };
        

        Action launchTheGameCallback = ()
        => Match3Events.CreateGameTask?.Invoke(GameManager.Instance.Level, GameFinishTaskGenerator.Instance.GetSortedGemWeights(GameManager.Instance.GemTypes), createGemTaskCallback);

        Match3Events.LaunchTheGame?.Invoke(launchTheGameCallback);
   } */
}
